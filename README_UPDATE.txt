# Kontakt PRO – paczka do podmiany
1) Podmień `contact.html` i `app.js` w projekcie (Vercel/GitHub).
2) Jeśli chcesz zapisy do bazy – w `config.js` wpisz `SUPABASE_URL` i `SUPABASE_ANON_KEY`.
3) Deploy → wejście na /contact.html i test.
4) Tryb DEMO działa bez kluczy (localStorage).
